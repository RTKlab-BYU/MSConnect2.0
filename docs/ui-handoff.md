# Retired UI Handoff

The Django-template UI is retired.

Keep the active browser contract in:

- `docs/app-review-guide.md`
- `README.md`

Legacy `/ui/*` redirects and intake permission rules remain as backend compatibility behavior, but new UI work should target `/app/*`.
